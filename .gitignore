# env
.venv/
__pycache__/
*.pyc

# data artifacts (b0 output — too big for git, regenerate via b0.py)
*.bin

# HuggingFace dataset cache (if it lands locally)
.cache/

# personal answer keys / learning aids — kept local, not for the public repo
*_reference.py